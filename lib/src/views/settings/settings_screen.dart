import 'package:flutter/material.dart';
import 'package:weather/src/views/settings/components/body.dart';

class SettingsScreen extends StatelessWidget {
  const SettingsScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Body();
  }
}
